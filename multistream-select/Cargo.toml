[package]
name = "multistream-select"
version = "0.1.0"
authors = ["Parity Technologies <admin@parity.io>"]

[dependencies]
bytes = "0.4"
futures = { version = "0.1" }
tokio-core = "0.1"
tokio-io = "0.1"
