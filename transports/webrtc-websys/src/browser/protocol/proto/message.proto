syntax = "proto3";
package signaling;

message SignalingMessage {
  enum Type {
    SDP_OFFER = 0;
    SDP_ANSWER = 1;
    ICE_CANDIDATE = 2;
  }

  optional Type type = 1;
  optional string data = 2;
}
